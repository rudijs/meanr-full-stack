require_relative '../spec_helper'

describe 'apt-get-periodic::default' do
  # let(:chef_run) { ChefSpec::Runner.new.converge(described_recipe) }

  # it 'runs apt-get update' do
  #  expect(chef_run).to run_execute('apt-get update')
  # end

end
